Вот ссылка на отчет: https://docs.google.com/document/d/1QKprBU5-8kR1edAq0zefo0QnSU_oHQJWn88UCcZiBxc/edit

В папке parsed texts лежат тексты с разметкой UDPipe

В папке tagged texts лежат тексты с разметкой (автоматичсеки сделанной нами) типа номер предложения -> номер токенов тональных -> Food/Service -> 0/1

Есть также два файла Food_words и Service_words с расширенным список тональных слова

Файл SentimentLex_Adagram содержит в себе скрипт, который расширяет список тональных слов (word_vocab.pickle это словарь с этими словами), с помощью адаграмма, SentimentLex расширяет список тональных слов с помощью метрик + использует полученный в скрипте с адаграммом словарь, в итоге получаем большой словарь (vocab.pickle) тональных слов.

Файл food_service_classifier содержит в себе скрипт, который использует список тональных слов и размеченные тексты для соствления разметки типа номер предложения -> номер токенов тональных -> Food/Service -> 0/1
